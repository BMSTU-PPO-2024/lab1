
export interface PutFeedData {
    visible?: boolean,
    ids?: string[],
    channelIds?: string[],
    name: string;
}
export interface UpdFeedData {
    visible?: boolean,
    ids?: string[],
    channelIds?: string[],
    name?: string;
}